// create a function that takes a list of non-negative integers and strings and returns a new list with the strings filtered out


let arr = [12, 13, 45, "ste", "boy", true]
const array = (n) =>{
    
    if (typeof(n) === 'number'){
        return n
    }
    console.log(arr.filter(array))
}